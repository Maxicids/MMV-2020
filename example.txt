function str concat[str a & str b]
{
        var str result = a + b.
        return[result].
}
head
{
	var str stra = "just ".
        var str strb.
        var str strc = concat[stra & strb]. 
        output [strc]. 
}